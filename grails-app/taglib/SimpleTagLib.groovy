 


/**
 * SimpleTagLib
 * A taglib library provides a set of reusable tags to help rendering the views.
 */
class SimpleTagLib {
    static namespace = "simplest"
    def bla1 = { attrs, body ->
        
        out << "bla"
    }
}
